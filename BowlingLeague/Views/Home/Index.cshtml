@{
    ViewData["Title"] = "Home Page";
}

@{
    ViewData["Title"] = "Home Page";
}

@model List<Bowler>

<h1 class="mb-4 ">Bowlers</h1>

<ul>
    @foreach (Bowler b in Model)
    {
    <li>@b.BowlerID - @b.BowlerFirstName @b.BowlerMiddleInit @b.BowlerLastName - @b.BowlerAddress @b.BowlerCity, @b.BowlerState @b.BowlerZip - @b.BowlerPhoneNumber </li>

    }
</ul>
